source "https://rubygems.org"

# Same Jekyll version GitHub Pages builds with, so local matches production
gem "jekyll", "~> 3.10.0"
gem "kramdown-parser-gfm"

# No longer bundled with newer Ruby versions, but Jekyll 3 still needs them
gem "base64"
gem "bigdecimal"
gem "csv"
gem "logger"
gem "webrick"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
